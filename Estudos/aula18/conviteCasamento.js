const convite = (nomeConvidado, data, horario, local, nomeNoivos) => {
    alert(`Você ${nomeConvidado} está sendo convidado para o nosso casamento\nNo dia ${data}, horário ${horario} e local ${local}\natenciosamente ${nomeNoivos}`)
}

let t1 = convite('Emerson', '20/09', '9 hrs', 'LA', 'noivoas' )